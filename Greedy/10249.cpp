#include<bits/stdc++.h>
#include<iostream>
#include<vector>
#include<algorithm>

using namespace std;

int main()
{
     int M , N;
     while(scanf("%d %d",&M,&N)==2)
     {
         if(M ==0 || N ==0)
         {
             break;
         }

         vector<int> team[M];
         vector<int> table[N];
         for(int i=0;i<M;i++)
         {
             cin >> team[i];
             // Team Inputs
         }

         for(int j=0;j<N;j++)
         {
             cin >> table[j];
             // table capacity
         }

    }
return 0;
}
