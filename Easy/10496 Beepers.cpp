#include<bits/stdc++.h>
#include<iostream>
using namespace std;
int positions[20][20];  // finding the distance bwn pts.
int x[20],y[20];
int main(){
     int t;
     cin >> t; // testcases

     while(t--){
          int x1,y1;
          cin >> x1 >> y1; // find the size of the board

          cin >> x[0] >> y[0];    // start position of karel
          int number;
          cin >> number;
          for(int i=1;i<=number;i++){
              cin >> x[i] >> y[i];     //
              positions[x[i]][]
          }
          positions[startx][starty]=0; // Initital position.
          // function to find the distance
          //
     }
return 0;}
